import React, { useEffect } from "react";
import { useSelector } from "react-redux";
import { useDispatch } from "react-redux";

const HoraireChaudiere = () => {
  return (
    <div>
      <h1>HoraireChaudiere</h1>
    </div>
  );
};

export default HoraireChaudiere;
